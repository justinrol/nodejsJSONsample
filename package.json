{
  "name": "myapp",
  "version": "1.0.0",
  "description": "my First App that will run a simple server",
  "main": "server-example.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.13.4"
  }
}
